#include<stdio.h>
int main()
{
    int t,n,k,i;
    scanf("%d",&t);
   while(t--)
    {
        scanf("%d%d",&n,&k);
        if(n<k)
            printf("<\n");
            else if(n>k)
                printf(">\n");
            else
                printf("=\n");
    }
    return 0;
}
